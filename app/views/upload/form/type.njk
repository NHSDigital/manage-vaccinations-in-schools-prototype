{% extends "_layouts/form.njk" %}

{% set title = __("upload.type.title") %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        heading: title,
        size: "l"
      }
    },
    items: typeItems,
    decorate: "upload.type"
  }) }}
{% endblock %}
