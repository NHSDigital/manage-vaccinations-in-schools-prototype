{% extends "_layouts/form.njk" %}

{% set title = __("upload.type.title") %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        isPageHeading: true,
        text: title,
        size: "l"
      }
    },
    items: typeItems,
    decorate: "upload.type"
  }) }}
{% endblock %}
