{% extends "_layouts/form.njk" %}

{% set title = __("contact.notify.title") %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        html: appHeading({
          caption: patient.fullName,
          title: title
        })
      }
    },
    items: getBooleanItems(),
    decorate: "reply.contact.notify"
  }) }}
{% endblock %}
