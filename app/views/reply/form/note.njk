{% extends "_layouts/form.njk" %}

{% set confirmButtonText = __("form.confirm") %}
{% set title = __("reply.note.title") %}

{% block form %}
  {{ textarea({
    label: {
      html: heading({
        caption: __("reply.show.title", reply.fullName),
        text: title,
        size: "l"
      })
    },
    decorate: "reply.note"
  }) }}
{% endblock %}
