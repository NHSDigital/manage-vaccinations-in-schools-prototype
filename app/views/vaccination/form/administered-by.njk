{% extends "_layouts/form.njk" %}

{% set title = __("vaccination.administeredBy.title") %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        html: appHeading({
          classes: "nhsuk-fieldset__legend--l",
          caption: vaccination.patient.fullName,
          title: title
        })
      }
    },
    items: userItems,
    decorate: "vaccination.administeredBy_uid"
  }) }}
{% endblock %}
