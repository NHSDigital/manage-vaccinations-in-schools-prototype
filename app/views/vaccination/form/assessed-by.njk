{% extends "_layouts/form.njk" %}

{% set title = __("vaccination.assessedBy.title") %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        html: heading({
          caption: vaccination.patient.fullName,
          text: title,
          size: "l"
        })
      }
    },
    items: userItems,
    decorate: "vaccination.assessedBy_uid"
  }) }}
{% endblock %}
