{% from "_macros/report-search.njk" import appReportSearch with context %}
{% from "report/_navigation.njk" import reportNavigation with context %}

{% extends "_layouts/form.njk" %}

{% set gridColumns = "full" %}
{% set hideConfirmButton = true %}
{% set title = __("report.list.title") + " – " + __("report.consent.title") %}

{% block form %}
  {{ super() }}

  {{ reportNavigation({
    view: "consent"
  }) }}

  <div class="nhsuk-grid-row">
    <div class="nhsuk-grid-column-one-quarter">
      {{ appReportSearch({
        clear: "/reports/consent"
      }) }}
    </div>

    <div class="nhsuk-grid-column-three-quarters">
      {{ __("report.list.updated", today) | nhsukMarkdown }}
      {{ link("/reports/consent-terms", __("report.consent-terms.title")) | nhsukMarkdown }}
    </div>
  </div>
{% endblock %}
