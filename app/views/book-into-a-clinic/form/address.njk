{% extends "_layouts/form.njk" %}

{% set title = __("clinicBooking.address.title") %}

{% block form %}
  {% call fieldset({
    legend: {
      html: appHeading({
        classes: "nhsuk-fieldset__legend--l",
        title: title
      })
    }
  }) %}
    <p class="nhsuk-hint">{{ __("clinicBooking.address.hint") }}</p>

    {{ input({
      label: { text: __("location.addressLine1.label") },
      autocomplete: "address-line1",
      decorate: "clinicBooking.appointments[child_index].child.address.addressLine1"
    }) }}

    {{ input({
      label: { text: __("location.addressLine2.label") + " (optional)" },
      autocomplete: "address-line2",
      decorate: "clinicBooking.appointments[child_index].child.address.addressLine2"
    }) }}

    {{ input({
      classes: "nhsuk-u-width-two-thirds",
      label: { text: __("location.addressLevel1.label") },
      autocomplete: "address-level1",
      decorate: "clinicBooking.appointments[child_index].child.address.addressLevel1"
    }) }}

    {{ input({
      classes: "nhsuk-input--width-10",
      label: { text: __("location.postalCode.label") },
      autocomplete: "postal-code",
      decorate: "clinicBooking.appointments[child_index].child.address.postalCode"
    }) }}
  {% endcall %}
{% endblock %}
