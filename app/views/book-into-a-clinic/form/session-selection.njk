{% extends "_layouts/form.njk" %}

{% set title = __("clinicBooking.sessionSelection.title", firstName) %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        html: appHeading({
          title: title,
          caption: __("clinicBooking.appointment.caption", fullName) if childCount > 1
        })
      }
    },
    hint: { text: __("clinicBooking.sessionSelection.hint") },
    items: previousSessionItems,
    decorate: "transaction.sessionChoice"
  }) }}
{% endblock %}
