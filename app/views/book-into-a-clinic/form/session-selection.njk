{% extends "_layouts/form.njk" %}

{% set title = __("clinicBooking.sessionSelection.title", firstName) %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        html: appHeading({
          title: title,
          caption: appointmentCaption
        })
      }
    },
    hint: __("clinicBooking.sessionSelection.hint"),
    items: previousSessionItems,
    decorate: "journeyData.sessionChoice"
  }) }}
{% endblock %}
