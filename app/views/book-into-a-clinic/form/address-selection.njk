{% extends "_layouts/form.njk" %}

{% set title = __("clinicBooking.addressSelection.title", firstName) %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        html: appHeading({
          title: title,
          caption: appointmentCaption
        })
      }
    },
    hint: __("clinicBooking.addressSelection.hint"),
    items: previousAddressItems,
    decorate: "journeyData.addressChoice"
  }) }}
{% endblock %}
