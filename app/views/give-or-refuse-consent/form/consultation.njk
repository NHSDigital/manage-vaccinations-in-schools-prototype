{% extends "_layouts/form.njk" %}

{% set title = __("consent.hasRequestedConsultation.title") %}
{% set hint = __("consent.hasRequestedConsultation.hint." + consent.refusalReason) %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        html: appHeading({
          classes: "nhsuk-fieldset__legend--l",
          title: title
        })
      }
    },
    hint: { text: hint } if hint,
    items: [{
      text: __("consent.hasRequestedConsultation.yes"),
      value: true
    },
    {
      text: __("consent.hasRequestedConsultation.no"),
      value: false
    }],
    decorate: "consent.hasRequestedConsultation"
  }) }}
{% endblock %}
