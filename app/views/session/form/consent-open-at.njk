{% extends "_layouts/form.njk" %}

{% set title = __("session.consentOpenAt.title") %}

{% block form %}
  {{ dateInput({
    fieldset: {
      legend: {
        html: heading({
          caption: session.location.name if type == "edit",
          text: title,
          size: "l"
        })
      }
    },
    hint: __("session.consentOpenAt.hint"),
    decorate: "session.consentOpenAt_"
  }) }}
{% endblock %}
