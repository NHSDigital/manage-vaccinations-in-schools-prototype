{% extends "_layouts/form.njk" %}

{% set title = __("school.name.title") %}

{% block form %}
  {{ input({
    label: {
      heading: title,
      size: "l"
    },
    decorate: "school.name"
  }) }}
{% endblock %}
